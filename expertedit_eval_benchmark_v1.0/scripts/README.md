# Scripts

- `load_evaluation_pairs.py` loads `data/evaluation_pairs.csv`, prints counts, and checks DTW alignment IDs.
- `validate_release.py` validates file presence, counts, checksums, absence of raw media, absence of absolute internal paths, and basic referential integrity.

Both scripts use only the Python standard library.
